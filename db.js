// Production database composition. Domain operations live in the injectable
// pool-backed factory; this module preserves the application's named-export API.
import pg from 'pg';
import { createDatabase } from './server/database.js';
import { databaseConnectionString } from './server/database-config.js';

const pool = new pg.Pool({ connectionString: databaseConnectionString() });
pool.on('error', (error) => console.error('[db] pool error:', error.message));

const database = createDatabase(pool);

export { createDatabase };
export const {
  close,
  listDecks,
  getDeck,
  insertDeck,
  updateDeck,
  listBoards,
  getBoard,
  insertBoard,
  updateBoard,
  listMats,
  getMat,
  insertMat,
  updateMat,
  listProps,
  insertProp,
  updateProp,
  listScenes,
  getScene,
  insertScene,
  listSkyboxes,
  insertSkybox,
  listDice,
  getDice,
  insertDice,
  allAssetRefBlobs,
  setAssetPublic,
  renameAsset,
  deleteAsset,
  createUser,
  bootstrapAdmin,
  changeAdminByLogin,
  findUserByLogin,
  findUserByToken,
  findUserById,
  createSession,
  revokeSession,
  revokeUserSessions,
  setPassword,
  setUserAvatar,
  listUsers,
  setAdmin,
  setHostStatus,
  countPendingHosts,
  roomsOwnedBy,
  purgeUser,
  createRoom,
  findRoomByCode,
  getRoom,
  listRoomsForUser,
  listRoomsForAdmin,
  listRooms,
  setRoomPolicy,
  renameRoom,
  getRoomState,
  saveRoomState,
  softDeleteRoom,
  restoreRoom,
  purgeRoom,
  joinRoom,
  getMembership,
  admitMember,
  kickMember,
  setMemberRole,
  listMembers,
} = database;
